
<div id="register-body">
    <div class='form registration form-column'>
        <form method='POST' id='reg-form'>
            <p class="form-header">Registration Form</p>
            <div id='name-fields'>
                <div class="input-div">
                    <label for="firstName">First Name:   </label>
                    <input id='firstName' name='firstName' type="text"  title="must contain characters" minlength="2">{{#if tryAgain}}*{{/if}}
                </div>

                <div class="input-div">
                    <label for="lastName">Last Name:   </label><input id='lastName' name='lastName' type="text" minlength="2">{{#if tryAgain}}*{{/if}}
                </div>
                <div class="input-div">
                    <label for="email">Email:   </label><input id='email' name='email' type="text" minlength="2">{{#if tryAgain}}*{{/if}}
                </div>
                <div class="input-div">
                    <label for="password">Password:   </label><input id='password' name='password' type="text" minlength="2">{{#if tryAgain}}*{{/if}}
                </div>
            </div>

            {{#if tryAgain}}
            <p id="errorMessage">Woops! Looks like something went wrong. Please try again.</p>
            <p>*<span id="errorMessage2">Mandatory fields</span></p>
            {{/if}}

            <input type="hidden" name="_csrf" value="{{csrfToken}}">

            <button class='submit submit-registration'>Register</button>
        </form>
    </div>

    <div class="info-column">
        <p>Has this ever happened to you?</p>
            <img src="/comic.png" alt="funny-comic" id="thx-comic">
        <p>Sign our petition to criminalize this nonsense!</p>
    </div>

</div>